server:
  port: 8181

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: service-customer
          uri: http://localhost:8184/customer
          predicates:
            - Path=/customers/**
        - id: service-product
          uri: lb://service-product
          predicates:
            - Path=/products/**
        - id: service-operations
          uri: lb://service-operations
          predicates:
            - Path=/operations/**
        - id: service-manage
            uri: lb://service-manage
            predicates:
              - Path=/manages/**